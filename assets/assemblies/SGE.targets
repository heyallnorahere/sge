<Project>

  <PropertyGroup>
    <OutDir>$(MSBuildThisFileDirectory)\$(Configuration)\$(AssemblyName)</OutDir>
  </PropertyGroup>

  <PropertyGroup Condition=" '$(Configuration)' == 'Debug' ">
    <DefineConstants>SGE_DEBUG</DefineConstants>
  </PropertyGroup>

</Project>